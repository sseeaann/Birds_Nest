<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Manage Contacts</title>
</head>
<body>
	<div class="container">
		<div class="panel panel-warning">
		  <div class="panel-heading">
		    <h3 class="panel-title">Baby Birds</h3>
		  </div>
		  <div class="panel-body">
				<table class="table table-striped table-hover ">
					<tr>
						<th>First Name</th>
						<th>Last Name</th>
						<th>Email</th>
						<th>Phone</th>
						<th>Address</th>
						<th>City</th>
						<th>State</th>
						<th>Zip</th>
					</tr>
					<tr>
		<% 			if @contacts.length > 0
								@contacts.each{ | contact | %>
						<td><%= contact.first_name %></td>
						<td><%= contact.last_name %></td>
						<td><%= contact.email %></td>
						<td><%= contact.phone %></td>
						<td><%= contact.address_street %> <%= contact.address_apt %></td>
						<td><%= contact.address_city %></td>
						<td><%= contact.address_state %></td>
						<td><%= contact.address_zip %></td>
						<td><a href="/contacts/<%= contact.id %>/edit" class="btn btn-warning panel-button">Edit Bird</a></td>
						<td><a href="/contacts/<%= contact.id %>" data-method='delete' class="btn btn-danger panel-button">Delete Bird</a></td>
					</tr>
			<%	} end %>
				</table>
				<a href="/contacts/new" class="btn btn-warning panel-button">Add Bird</a>
		  </div>
		</div>
		<a href="/users/show" class="btn btn-warning panel-button">Back</a>
	</div>
</body>
</html>