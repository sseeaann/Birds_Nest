<div class="container">
<h2>Resend unlock instructions</h2>

<%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }, wrapper: :horizontal_form) do |f| %>

    <%= f.input :email, autofocus: true %>
    <%= f.button :submit, "Resend unlock instructions", class: "btn btn-default" %>

<% end %>

<%= render "devise/shared/links" %>
</div>